# Runtime dependencies for Simple-Pluto-RADAR.
#
# pyadi-iio pulls in pylibiio, which is only a ctypes wrapper around the
# native libiio shared library. libiio itself is NOT installed by pip --
# see docs/usage.md for the per-OS install step.

numpy>=1.20
pyadi-iio==0.0.21

# visualize.py only; main.py and diagnose.py do not need it.
matplotlib>=3.5

# GUI toolkit for the live plot window. matplotlib cannot open a window
# without one -- with no toolkit installed it falls back to a file-only
# backend and plt.show() silently does nothing. PyQt goes in the venv, so
# no system packages are needed (tkinter cannot be pip-installed).
PyQt5>=5.15
